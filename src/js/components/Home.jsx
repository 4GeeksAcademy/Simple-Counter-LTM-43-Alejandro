import React from "react";
import SecondsCounter from "./SecondsCounter.jsx";

//include images into your bundle

//create your first component
const Home = () => {
	return (
	<div>
		<Home/>	
	</div>
		);
};

export default Home;

// Done. Crear el componente
// Done. Contar segundos (validar la funtion set interval tegnga un contador de segundo)
// cuando cambie los segundos renderizar el componente
// Done. pasar los segundos al SecondsCounter
// Tomar un numero (3434) y obtener los digitos 003434
// hacer la parte grafica